# yaml-language-server: $schema=./schema.json
$schema: ./schema.json
app:
  swagger:
    title: <% landscape %> <% projectName %>
  landscape: <% landscape %>

databases: <% databases %>

caches: <% caches %>

storages: <% storages %>

